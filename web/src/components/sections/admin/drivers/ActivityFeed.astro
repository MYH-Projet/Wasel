---
import { History } from "lucide-react";

interface Props {
    driverId: string | undefined;
}
const { driverId } = Astro.props;

// Simulate API Fetch
await new Promise((r) => setTimeout(r, 1500));

const activities = [
    { title: "Completed mission #1234", time: "2 hours ago", type: "mission" },
    {
        title: "Updated Insurance Document",
        time: "Yesterday, 14:20",
        type: "system",
    },
    {
        title: "Profile updated via Mobile App",
        time: "3 days ago",
        type: "system",
    },
    { title: "Completed mission #1209", time: "Oct 12, 2023", type: "mission" },
];
---

<div class="bg-white rounded-xl shadow-sm p-6 h-full flex flex-col">
    <h2 class="text-lg font-bold text-slate-900 flex items-center gap-3 mb-8">
        <span class="p-2 bg-orange-100 text-orange-700 rounded-lg">
            <History className="w-5 h-5" />
        </span>
        Activity Feed
    </h2>

    <div
        class="relative border-l-2 border-slate-100 ml-3 space-y-8 flex-grow mb-8"
    >
        {
            activities.map((activity) => (
                <div class="relative pl-6">
                    <span
                        class={`absolute -left-[9px] top-1.5 w-4 h-4 rounded-full border-4 border-white ${
                            activity.type === "mission"
                                ? "bg-yellow-700"
                                : "bg-slate-200"
                        }`}
                    />

                    <p class="text-sm font-medium text-slate-800">
                        {activity.title}
                    </p>
                    <p class="text-xs text-slate-500 mt-0.5">{activity.time}</p>
                </div>
            ))
        }
    </div>

    <button
        class="w-full py-2.5 border border-slate-200 text-slate-700 text-sm font-medium rounded-lg hover:bg-slate-50 transition-colors mt-auto"
    >
        View Full History
    </button>
</div>
